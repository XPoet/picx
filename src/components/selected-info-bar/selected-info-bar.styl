.selected-info-bar-box {
  display flex
  align-items center
  justify-content flex-start
  font-size 14rem

  .dir-name {
    display flex
    align-items center
    justify-content flex-start
    margin-right 6rem
    white-space nowrap

    &.upload {
      cursor pointer
    }

    .el-icon {
      margin-right 4rem
      font-size 16rem
    }
  }
}
