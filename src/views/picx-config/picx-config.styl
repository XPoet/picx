.config-page-container {
  .operation {
    display flex
    flex-direction column
    align-items flex-end

    .el-button {
      margin-left 20rem

      &:first-child {
        margin-left 0
      }
    }
  }

  .refresh-icon {
    display flex
    align-items center
    justify-content flex-end
    font-size 20rem
    cursor pointer
  }
}
